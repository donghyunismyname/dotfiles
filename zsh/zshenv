# Skip /etc/zsh/zshrc's global compinit on Linux. We run our own compinit -u
# in .zshrc, which also handles the insecure-directory case caused by shared
# linuxbrew (brew is single-user by design, so brew's zsh site-functions end
# up owned by another user).
skip_global_compinit=1
